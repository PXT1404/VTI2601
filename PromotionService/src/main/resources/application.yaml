spring:
  application:
    name: PromotionService
  datasource:
    url: jdbc:mysql://localhost:3306/promotion_service?createDatabaseIfNotExist=true
    username: root
    password: root
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    fetch-registry: true
    register-with-eureka: true
server:
  port: 8880
